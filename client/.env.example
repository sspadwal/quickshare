# Phones cannot use "localhost" — they need your PC's LAN IP (same Wi‑Fi).
# Copy to .env and set your address (ipconfig / ifconfig). Restart Vite.

VITE_PUBLIC_APP_ORIGIN=http://192.168.1.5:5173
VITE_BACKEND_URL=http://192.168.1.5:5000
